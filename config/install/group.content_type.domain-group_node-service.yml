langcode: en
status: true
dependencies:
  config:
    - group.type.domain
    - node.type.service
  module:
    - gnode
    - node
id: domain-group_node-service
label: 'Domain: Group node (Service)'
description: 'Adds <em class="placeholder">Service</em> content to groups both publicly and privately.'
group_type: domain
content_plugin: 'group_node:service'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
