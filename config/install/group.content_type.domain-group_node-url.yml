langcode: en
status: true
dependencies:
  config:
    - group.type.domain
    - node.type.url
  module:
    - gnode
    - node
id: domain-group_node-url
label: 'Domain: Group node (URL)'
description: 'Adds <em class="placeholder">URL</em> content to groups both publicly and privately.'
group_type: domain
content_plugin: 'group_node:url'
plugin_config:
  group_cardinality: 5
  entity_cardinality: 1
  use_creation_wizard: false
